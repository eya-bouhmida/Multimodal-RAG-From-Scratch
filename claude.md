# Changelog

- Backend: ajout de `backend/config.py`, `backend/schemas.py` ; ajout de `QDRANT_URL` dans `.env` / `.env.example`.
- RAG: ajout de `src/retriever.py` (HybridRetriever: dense+BM25 pagination complète+RRF+rerank+image_search).
- RAG: ajout de `src/generator.py` (MedLensGenerator: prompt bilingue + génération streaming Groq).
- Backend: ajout de `backend/app.py` (routes `/api/health`, `/api/chat` en SSE, CORS, lifespan) + `backend/requirements.txt`.
- Backend: test local OK (`/api/health` → 38214 chunks texte, `/api/chat` streaming avec sources). Modèle Groq changé pour `openai/gpt-oss-20b` (llama-3.1-8b-instant retiré de Groq).
- Frontend: scaffold Vite+React dans `frontend/`, proxy `/api` vers le backend.
- Frontend: composants chat (`ChatWindow`, `MessageBubble`, `ChatInput`, `SourcesList`), `api/chat.js` (parsing SSE), `App.jsx`/`App.css` (UI MedLens).
- Test end-to-end OK : question EN et FR via le proxy frontend → backend, réponse streamée bien fondée sur les documents avec sources.
- Prompt: retrait des citations inline (plus de "(filename p.X)" dans les réponses) ; frontend n'affiche plus de liste de sources (suppression de `SourcesList.jsx`).
- Upload de document: ajout de `src/parser.py` (extraction PDF + chunking), `HybridRetriever.search_local_chunks` (similarité cosinus en mémoire), routes `/api/upload` et `/api/upload/{session_id}` dans `backend/app.py`, `generator.build_prompt` intègre les extraits uploadés en plus de la base Qdrant.
- Frontend: composant `DocumentUpload.jsx` + `api/upload.js`, `sessionId` généré par onglet et envoyé sur `/api/chat` et `/api/upload`.
- Test upload OK : PDF de test avec info fictive → réponse correcte tant que le document est actif, puis "je ne trouve pas cette info" après suppression via `/api/upload/{session_id}`.
- Design: palette vert pastel (`App.css`), zone d'upload de document déplacée dans la barre de saisie (`ChatInput.jsx` intègre désormais `DocumentUpload`).
- Design: page carte flottante avec fond dégradé (`index.css`), état vide amélioré avec suggestions cliquables (`ChatWindow.jsx`), avatars sur les bulles (`MessageBubble.jsx`), en-tête avec logo.
- Résumé de document: `/api/chat` envoie désormais tout le document uploadé (plus de recherche top-k), donc "résume ce document" ou "résume la partie sur X" fonctionne directement dans le chat normal. Suppression du code mort `search_local_chunks`/`embed` dans `src/retriever.py`.
- Export PDF: nouveau `src/pdf_export.py` (reportlab) + route `/api/export-pdf` ; bouton "📄 Download as PDF" sous chaque réponse de l'assistant (`MessageBubble.jsx`, `api/pdf.js`). Testé : résumé fidèle au document uploadé + PDF généré valide avec mise en forme (gras).
- Fix: erreur 413 Groq (limite gratuite 8000 tokens/min) quand un document volumineux était envoyé en entier à chaque tour. `src/generator.py` plafonne maintenant le contenu du document uploadé (~2500 tokens, top_k réduit, max_tokens de sortie réduit) quand un document est actif.
- Gros documents: ajout de `select_relevant_chunks` (BM25 local, `src/retriever.py`) pour choisir les extraits les plus pertinents par rapport à la question avant de les passer au générateur, au lieu de toujours prendre les premiers du document. Testé : info cachée en page 10/15 correctement retrouvée ; petit document toujours résumé en entier.
- Sécurité: détection de questions à caractère urgent (`is_emergency_query`, mots-clés FR/EN dans `src/generator.py`) → message fixe orientant vers les urgences (15/112) affiché avant la réponse RAG habituelle, sans jamais inventer de contenu médical. Testé sur le cas ingestion de javel + non-régression sur une question normale (pas de faux positif).
- Ajout du numéro d'urgence tunisien (190 SAMU) dans le message d'urgence.
- Tests: ajout de `pytest.ini` + `requirements-dev.txt`, suite de 20 tests (`tests/test_parser.py`, `test_retriever.py`, `test_generator.py`, `test_pdf_export.py`, `test_api.py` avec retriever/generator factices pour ne pas appeler Qdrant/Groq en vrai). Un test a révélé un vrai petit bug dans `chunk_text` (chunk vide possible en fin de texte) — corrigé dans `src/parser.py`. Tous les tests passent (`python -m pytest tests/`).
- Multimodal visuel: vérifié que Qdrant ne stocke que `image_path` (pas les bytes) → les fichiers doivent être présents localement. Ajout du service statique `/api/images/{filename}` (`backend/app.py`, dossier `data/processed/images/`, gitignored), `generate_stream` renvoie maintenant `images: [{filename, caption}]` dans l'event `done`, le frontend affiche les vignettes sous la réponse (`MessageBubble.jsx`) avec repli silencieux si le fichier est absent. Testé : métadonnées bien renvoyées, route image répond (404 tant que les fichiers ne sont pas copiés en local).
- Filtrage qualité des images: 200 légendes Qdrant analysées par mots-clés (charts/graphs/diagrammes médicaux vs logos/couvertures/icônes) → 171 retenues dans `data/processed/image_whitelist.json`. `HybridRetriever.image_search` filtre maintenant nativement sur Qdrant (`Filter`+`MatchAny` sur `filename`) pour ne jamais renvoyer les images écartées. Nécessite un index Qdrant sur le champ `filename` (créé une fois via script). Testé : seules des images de la liste blanche sont retournées ; suite de tests toujours verte.
- Multimodal visuel opérationnel : dossier `data/processed/images/` peuplé en local (extraction brute complète, 35869 fichiers) ; les 171 images de la liste blanche sont toutes présentes. Testé de bout en bout : route `/api/images/{filename}` (directe et via proxy frontend) renvoie 200, les vignettes s'affichent dans le chat.
- Déploiement (prépa) : les 171 images utiles copiées dans `backend/static/images/` (commité, ~37 Mo) pour ne pas dépendre du dossier local ; `backend/config.py` (CORS via env `CORS_ORIGINS`) ; `frontend/src/api/config.js` (`VITE_API_URL`) utilisé par `chat.js`/`upload.js`/`pdf.js`/`MessageBubble.jsx` pour pointer vers le backend en prod ; ajout de `render.yaml`. Testé en local que rien n'est cassé (tests + backend + proxy frontend). Premier commit + push du projet complet sur `origin/main`.
- Déploiement Render échoué (out of memory, 512 Mo tier gratuit) même après avoir forcé torch CPU-only (`backend/requirements.txt`). Railway écarté (plus vraiment gratuit). Ajout de `railway.json` (gardé au cas où, non utilisé pour l'instant).
- Fix mémoire définitif : suppression du reranker cross-encoder (`src/retriever.py`, `HybridRetriever`) — `hybrid_search` garde dense+BM25+RRF, retourne directement les résultats fusionnés au lieu de les reclasser. Enlève un modèle transformer entier de la RAM. Testé : réponses toujours pertinentes, sources et images cohérentes, suite de tests verte.
- Diagnostic mémoire précis (instrumentation temporaire) : sentence-transformers/torch = ~400 Mo rien qu'à l'import → remplacé par `fastembed` (ONNX, même modèle, vérifié numériquement identique — similarité cosinus 1.000000 sur plusieurs phrases réelles, même classement de recherche) → ~200 Mo pour l'embedding. Reste : tokeniser 38214 chunks en listes de mots Python coûtait à lui seul ~430 Mo (overhead d'objet par mot) → remplacé `rank_bm25.BM25Okapi` par `CompactBM25` (nouvelle classe dans `src/retriever.py`, même formule BM25Okapi exacte, matrice creuse scipy + `array` typés au lieu de dicts Python). Vérifié sur les 38214 vrais chunks : classements identiques à `rank_bm25`, écarts de score ~1e-6 (bruit float32). Stockage des chunks aussi allégé (`_ChunkRecord`, seulement text/filename/page_num). RAM du process : 1223 Mo → 528 Mo. Suite de tests verte, réponses identiques en local.
- 528 Mo encore trop juste sur Render (out of memory à nouveau). Ajout d'un flag `ENABLE_BM25` (`backend/config.py`, `HybridRetriever(enable_bm25=...)` dans `src/retriever.py`) : un seul code, deux modes selon l'env. En local (`.env` sans la variable) → hybride complet comme toujours (528 Mo, testé). Sur Render (`ENABLE_BM25=false` dans `render.yaml`) → recherche dense uniquement (Qdrant), saute complètement le scroll+BM25 → 314 Mo mesuré en local, testé fonctionnel (réponses toujours correctes et bien sourcées, juste sans la moitié BM25 du hybrid search).
- **Déploiement terminé et fonctionnel** : backend live sur Render (https://multimodal-rag-from-scratch.onrender.com, mode dense-only), frontend live sur Vercel (https://multimodal-rag-from-scratch.vercel.app). `CORS_ORIGINS` mis à jour sur Render avec l'URL Vercel + localhost. Testé de bout en bout en production : `/api/health` OK, `/api/chat` répond correctement avec sources, header `access-control-allow-origin` confirmé présent pour l'origine Vercel.
